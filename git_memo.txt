【Gitで保存するとき】
git ini #Gitを使えるようにする

cd "C:/Users/muuma/.gemini/antigravity/scratch/anti-exness"

pwd

git remote add origin https://github.com/muman84g/anti-exness.git
git branch -M main
git add .
git commit -m "???"
git push -u origin main


【GitからDLするとき】
git clone https://github.com/muman84g/anti-exness.git
gitgit

【変更したデータを上書き】
git status#変更部が赤文字であることを確認

git add .#変更データを確認

git status#変更部が緑になったことを確認

git commit -m "内容を記載"#セーブ

git push

