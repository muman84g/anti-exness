# Source Backtest

## Bot24 execution evidence and bounded logging (2026-09-03 local correction)

- The mandatory execution ledger now uses a bot24-specific 29-column schema
  with lane/magic, opportunity/basket, separate ticket/position/deal identities,
  entry/exit prices and causal event/release/available/decision/executable clocks.
- Core is recorded as lane 1 / magic `200024`; the independent v206 strategy is
  recorded as lane 206 / magic `240206`. This keeps simultaneous independent
  entries distinguishable without changing their signal or lifecycle rules.
- Repeated synchronization/data diagnostics are summarized on a bounded cadence,
  and `s24_bot.log` uses 10 MiB size rotation with five backups.
- This is evidence/operations hardening only. Signal conditions, entry timing,
  lots, capacity, TP/SL, holds, cooldowns, routing and broker commands are unchanged.
- The repository's prior self-test fixture rows and diagnostic log were not live
  history. The canonical upload files were reset to a header-only execution CSV
  and an empty process log before validation.

## Added independent v206 lane (2026-09-01 local implementation)

- Frozen candidate: `man_237_v206 / path_monotonic_center_approach`.
- Research source SHA256: `3c1f2701835e202b9c0c7ddb478c29b72ebf34581d68986be13c46329a384996`.
- Exit source SHA256: `6276b5ed3be24c5afaf757a0d4597eef37f7dfbbdaee360a6598d27fa1d5a1db`.
- This lane does not replace or modify the bot24 core candidate below. It has private lane 206, magic `240206`, comment `s24_v206`, and separate lifecycle state.
- Full-history live signal code matched frozen research exactly: DEV 170, observed Leakcheck 37, Forward 4. Runtime-bounded 1000-M1 reconstruction was also exact on every scoped bar (91,926 / 25,642 / 4,091; mismatch 0 / 0 / 0).
- Entry occurs after a completed M1 signal. Broker-side fixed SL is submitted with the market order, TP is set to exact 1R from actual fill, and the 30-minute timeout begins at the broker-confirmed fill time and is evaluated only from a fresh broker quote clock. Crash recovery never treats a confirmed fill with missing TP as no fill.
- Before OPEN, the runner refreshes quote and symbol metadata at the submission boundary, persists a full opportunity/ownership/lot receipt, and re-queries exact positions and orders after every response. Unresolved OPEN/CLOSE is not automatically resent. Disabling v206 stops new entries but does not hide existing v206 inventory from reconciliation and close monitoring.
- The local execution guard permits the same frozen signal to wait at most two minutes for cooldown/spread recovery and rejects executable spread above 300 points. Those guards were not independently scored as research filters, so exact signal parity is not proof of identical live trade supply or PnL.
- Local implementation only: no deployment, restart, EA attach, state mutation, or live order was authorized or performed.
- Runtime poll and status-log intervals are frozen at 5 and 60 seconds and validated at construction. Invalid text, null, non-finite, zero or negative values are rejected before any broker poll, preventing a post-decision crash or zero-delay busy loop without changing trading logic.

## Core execution safety closure (2026-09-01 local)

- The frozen core signal, lots, add rule, basket exits and confirmed-M1 cadence are unchanged.
- Core market OPEN now carries expected account login/server and expected owned-position count into the bridge. The bridge atomically rechecks hedging mode, permissions, symbol policy, margin, exact namespace inventory and empty bot-owned orders immediately before `CTrade`.
- Each new core order uses an opportunity-derived comment under `s24_no_adverse:`. Legacy exact `s24_no_adverse` positions remain recognizable and closeable, but are never adopted merely because they appeared after a definitive no-fill response.
- Core CLOSE now atomically rechecks ticket, symbol, magic, exact persisted comment, position identifier, side, volume, account, mode, permissions and empty bot-owned orders. A durable per-ticket submission-start marker prevents blind resend; state becomes flat only after exact absence plus aggregate CLOSEDEAL evidence.
- HIST timestamps are explicit Unix seconds interpreted as UTC. This removes the former undocumented assumption that formatted MT5 server wall time was UTC.
- Bridge v8 wraps each command in an atomically published request-ID/UTC-expiry envelope, claims it before execution, and requires a matching response ID. This is transport hardening only and does not alter the frozen signal or order sizing.
- Time-driven core and v206 closes defer the first attempt when spread exceeds 300 points, require three distinct fresh acceptable quotes after a wide quote, force release after 30 minutes, and retain ownership across a market-closed retry delay of 60 seconds. Malformed defer-only state is normalized without discarding the owned basket.
- Atomic close-guard failures are definitive no-fill: core clears only the affected ticket's submission marker and v206 clears only its pending-close receipt, while both retain a durable non-recoverable block that prevents automatic resubmission. Timeout, malformed, unknown, and execution-bearing outcomes continue to retain their unresolved receipt. Per-lane malformed state is replaced only with a blocked lane default; corrupt top-level identity remains a full fail-closed condition. Frozen execution and time-routing parameters are checked before any broker connection.
- For an exactly absent owned position, core and v206 first query CLOSEDEAL from the persisted broker-open window. An explicit no-deal result receives one bounded wider-history retry; transport/query failure remains distinct from a confirmed zero-deal result.
- Rejected per-lane state is preserved as a quarantine snapshot. Active basket or in-flight OPEN/CLOSE evidence in a rejected core snapshot makes startup fatal; v206 retains its rejected snapshot behind the migration gate. Temporary M1 history loss blocks every new signal and new exit decision but still permits exact inventory reconciliation and retry of a close reason already persisted by an earlier confirmed-M1 evaluation.
- A temporary INFO/quote failure still performs quote-less exact POSITIONS/ORDERS/CLOSEDEAL reconciliation for core and v206, but cannot advance timeout/spread/entry decisions. Active v206 lifecycle evidence retained in quarantine cannot be cleared by flat-confirmation migration; valid core pending-close reason and signal-bar identity are state-load invariants.
- Passive shadow-runner rows and auxiliaries are validated before polling. Invalid no-order shadow state is preserved separately and only the passive lane is reset, while an owned core basket remains available for reconciliation and exit management. Invalid quarantine containers and non-list core baskets are handled by the fail-closed loader rather than escaping as startup exceptions.
- Turning `live_trading_enabled` off blocks new real submissions but does not make an existing broker-backed core basket shadow inventory. The live/core-shadow switches and passive-shadow enable switches require JSON booleans; live and core-shadow entry cannot both be enabled. When both core execution switches are false, the runner is reconciliation-only and consumes/logs signals without creating simulated baskets. Exact owned reconciliation continues, due close identity is persisted without sending or locally clearing the position, and the recoverable `live_disabled_with_owned_inventory` block clears only after live execution is re-enabled and a complete active owned/order-clean sync succeeds. Shadow and broker-backed rows are validated as mutually exclusive basket modes.
- Account login/server identity, hedging mode and INFO broker quote clock are startup invariants even when live entry is disabled, because core/v206 durable ownership reconciliation still reads broker state. The switch therefore cannot make a wrong or legacy terminal eligible for lifecycle reconciliation.
- Runtime INFO keeps the same mandatory quote-clock contract on every poll. Missing, invalid, stale, future or non-monotonic quote time blocks decision advancement but still runs quote-less exact core/v206 inventory and CLOSEDEAL reconciliation. Exact owned sync restores broker open price and open time into both lane states, recalculates the v206 timeout from the broker open time, and restores the core latest-add reference from the newest confirmed fill. A malformed active core peak accumulator is reset for conservative rebuild from the next executable quote rather than discarding owned lifecycle evidence. Core and v206 persist one deduplicated decision receipt per completed M1 opportunity, including an explicit `not_evaluated_data_unavailable` outcome during an M1 outage. Newly opened core positions also persist their originating signal bar; a completed basket retains the exact available entry-signal-bar set, while v206 retains its closed position's signal bar. V10 state files gain only an empty evidence container on load, so existing broker-owned inventory remains reconcilable. These are lifecycle/evidence corrections only; frozen signal, lot, add threshold and exit thresholds are unchanged.
- Core state now carries `position_signal_identity_required`. Current-generation baskets require a valid signal bar on every position. A known legacy active basket that lacks this evidence remains available for exact sync and close, but new entries/adds stay blocked until that basket becomes flat; full close advances the lane to the strict current-generation contract.
- Core live ownership now requires the persisted executable ticket and lifecycle position identifier to match as a pair. Before OPEN, every existing broker row must match the durable basket and both fields must be positive and unique; after OPEN, the complete returned inventory is checked again before a fill can be adopted. This closes an identity/reconciliation gap only and does not change the frozen signal, lot, add rule, or exit thresholds.
- Exact MT5 OPEN retcodes 10018/10026/10027 are accepted as definitive no-fill only with zero order/deal receipts and a complete flat inventory delta check. v206 performs its complete post-OPEN namespace check before adopting a confirmed fill. Core/v206 state loading now rejects mixed-side or overlapping OPEN lifecycle evidence, orphan CLOSE intent/receipt, malformed reconciliation counters/control fields, and partial confirmed-close receipts. These are fail-closed lifecycle corrections only; no signal, entry routing, lot, add rule, TP/SL, hold, or cooldown parameter changed.
- After that exact retryable no-fill proof, core/v206 clear only the submission receipt and retain the still-valid signal/opportunity. A resend requires a distinct broker quote at least 60 seconds later; the same quote cannot submit twice. Atomic permission retcodes 10026/10027 are counted per lane and the third consecutive rejection raises the existing durable non-recoverable/manual-alert path. V14 state receives empty retry/count defaults, while partial or expired retry identity fails closed. This changes execution retry lifecycle only; signal, routing, lot, add, TP/SL, hold and cooldown remain frozen.
- Atomic OPEN guards other than the explicitly retryable permission rejection are definitive no-fill only for receipt cleanup; account, mode, ownership, policy, symbol, margin, bridge-inventory-query and invalid-request guards now create a durable non-recoverable/manual-alert block in both core and v206. Persisted core/v206/shadow timestamps require ISO strings, boolean financial or ownership identities cannot pass numeric coercion, malformed nested validators return stable fail-closed reasons, and corrupt v206 protection targets are rejected before protection reconciliation. These are execution/state-integrity corrections only and do not alter signal, routing, lot, add, TP/SL, hold or cooldown.
- Standalone opportunity-observer state, opportunity/markout CSV, state-tagger CSV and shadow-runner CSV are no-order evidence components. Initialization/schema/write failures preserve their original files, disable only the affected passive component and no longer prevent core/v206 startup or skip the core strategy poll. Disabled observer fallback uses inert defaults and does not reread rejected evidence; later observer/tagger runtime failures also disable that component for the process. The execution trade CSV remains mandatory and fail-closed. Malformed clean-sync related-ticket details reset confirmation progress and retain the existing block without raising. No signal, lot, exit or routing behavior changed.
- Unexpected v206 poll and quote-less reconciliation exceptions now use a transactional lane boundary: structurally valid durable submission or close receipts are retained, malformed partial state is quarantined and restored from the pre-poll valid snapshot, and a durable exception block is then saved. A shadow-runner poll exception restores the entire pre-poll no-order lane before disabling it, preventing a later core save from persisting half-mutated passive evidence. These corrections preserve broker-command receipts and do not alter signal, lot, exit or routing behavior.
- Bot24 holds an OS-released singleton runner lock on `state/s24_runner.lock` before constructing the live runner or connecting to the bridge. The per-command IPC lock remains separate: it serializes a command but cannot prevent two stale in-memory state owners from overwriting the same state file. A second process fails startup, while process termination automatically releases ownership even if the lock file remains. Self-test does not claim the live state/order namespace. No trading parameters or decision behavior changed.
- Confirmed core/v206 closure now persists close time, side, reason and the latest consumed signal bar. A same-direction completed-M1 signal whose execution due time was already known at or before that close is consumed rather than reused after restart; opposite-side reverse eligibility is unchanged. Incomplete persisted close identity fails closed. This is a lifecycle idempotency correction and does not change the frozen signal or thresholds.
- The per-command Python IPC lock now uses an OS-released advisory lock on both Windows and CentOS; a surviving lock file is inert and cannot permanently block the bridge after an abnormal process exit. Confirmed core OPEN/CLOSE ownership is persisted before mandatory execution-CSV output, and full-close reconciliation captures the original entry signal-bar set before clearing the basket. The request/response protocol, signal, routing, lot, add rule, TP/SL, hold and cooldown are unchanged.
- `--self-test` now redirects all state loading, normalization and saving to a temporary state file, and the safety regression suite isolates `STATE_FILE` per test. Validation therefore cannot migrate or rewrite the configured bot24 state. The state touched by the discovery run that exposed this defect was not guessed or repaired. No runtime lifecycle or trading behavior changed.
- Proven no-fill core OPEN outcomes and retryable MARKET_CLOSED/TRADE_PERMISSION core CLOSE outcomes now persist cleared submission receipts plus their retry or non-recoverable decision before writing the mandatory audit CSV. A CSV failure can still stop the runner, but cannot leave a resolved command permanently looking in-flight. Broker commands, retry timing, escalation counts, signals, thresholds, routing, lots and exits are unchanged.
- Mandatory execution-CSV validation now streams every existing data row with strict parsing and exact field-count checks during preflight and the process's first append. A truncated, over-wide or syntactically malformed row fails closed before broker execution or further append and is not modified automatically. Header, fields and normal CSV output are unchanged; passive shadow evidence retains its independent quarantine/disable boundary.
- Mandatory execution-CSV creation now distinguishes a genuinely absent path from an existing zero-byte file. The absent path receives the canonical header and first row; an existing empty file is treated as lost/corrupt execution evidence and fails closed without automatic reinitialization. No existing CSV or runtime state is repaired by this rule.
- Mandatory execution-CSV integrity is revalidated before every append, not cached for the process lifetime. If a previously validated or process-created path disappears, or if its schema/data rows are replaced while the runner remains alive, the next append fails closed without recreating or modifying that evidence. Trading parameters and order policy are unchanged.
- Mandatory execution-CSV validation now rejects semantically invalid full-width rows, including duplicate headers embedded as data, missing execution identity, invalid or non-UTC event timestamps, malformed optional signal timestamps, invalid live/side/ticket fields and nonfinite numeric evidence. The pending row is validated before opening the file for append. Current historical rows and the frozen trading contract are unchanged.
- Mandatory execution-CSV append now flushes and fsyncs the file before reporting success; first creation additionally fsyncs the parent directory where supported. Durability errors propagate instead of allowing state-persisted lifecycle evidence to be reported as successfully logged. Passive shadow CSV policy and every trading parameter remain unchanged.
- Mandatory execution-CSV creation now uses exclusive create, and existing-file validation plus append use one descriptor. A path created after the absence check, a disappearance during append, inode/path replacement, or size/mtime mutation during validation fails closed without recreating or appending to the unverified target. Signal, ownership, state and order policy are unchanged.
- Core comment ownership now accepts only exact legacy `s24_no_adverse` or the generated `s24_no_adverse:<10 lowercase hex>` form. Empty, non-hex, wrong-length, nested-delimiter and mere-prefix comments are rejected consistently by state validation, broker ownership, Python OPEN/CLOSE policy and the MQL5 bridge. v206 namespace and all trading parameters are unchanged.
- Execution-bearing MQL5 Bridge commands require strict unsigned integer or plain unsigned decimal text before numeric conversion. Trailing junk, signs, exponent notation, empty values and malformed decimals fail the command guard before any trade API call. These v10 command-field guards remain unchanged in v11; signal, lot, exit and routing behavior are unchanged.
- The MQL5 request-envelope expiry requires unsigned integer text before conversion. Malformed or suffixed expiry text fails `BAD_REQUEST_ENVELOPE` before command dispatch. This v11 envelope guard remains unchanged in v12; prior command-field guards and all trading behavior are preserved unchanged.
- The remaining MQL5 query commands now require exact arity and strict numeric text. `INFO/HIST` are pinned to XAUUSD, `HIST` to M1, and position/order inventory queries to bot24 core or v206 magic. This prevents a modified payload from returning another instrument's quote/bars without response-side symbol identity. Bridge identity was v12; all V31/V32 execution and envelope guards remained unchanged.
- Bridge v13 applies exact zero-argument arity to `ECHO`, `CAPS` and `ACCOUNT`, so extra payload fields cannot be silently ignored. Python bridge command/response/lock filenames and suffixes are local ASCII basenames, and the three IPC artifacts must remain distinct under case-insensitive filesystem semantics. No command behavior, signal, routing, lot or exit parameter changed.
- Passive observer/tagger artifact names are constrained to their configured log/state directories. Observer horizons and retention require strict positive integers; contract size and lot require positive finite numbers. The state tagger rejects non-finite or inverted historical OHLC before writing a row. These are no-order evidence-integrity guards only and do not alter trading decisions.
- Passive observer state now requires the complete version/symbol/pending/completed schema, valid UTC timestamps, strict non-boolean finite financial fields and JSON-safe metadata before any evidence reconciliation. Observer/tagger CSVs reject over-wide or truncated existing rows both at initialization and immediately before a later append, so post-start evidence corruption is preserved rather than extended. The live wrapper no longer coerces a boolean passive lot into a numeric value; an invalid observer configuration disables only that no-order component with inert fallback sizing. These are passive evidence-integrity corrections only; core/v206 signal, routing, lot, order and exit behavior remain unchanged.

- Bot: `bot24` / S24
- Candidate: `visual_no_adverse_c:target16`
- Idea/candidate: `man_028_v002` / `bot23_fixed3_trade_preserving_exit_opt_v2`
- Frozen hash: `54324fec28b2d9bffb9e01f73e5181d74e6d6a3d377fb86c1373934d0377be2f`
- Archived source: `backtest/bot関連backtest/backtest152/archive/bot23_fixed3_opt2_20260814/`
- Historical input: raw ordered XAUUSD Bid/Ask ticks, with signals derived from confirmed M1
- Historical visual-candidate execution: ticks aggregated to M1; basket exits evaluated once per M1; fills taken from the next minute's first Bid/Ask with configured slippage
- Former intended live execution: confirmed-M1 entry/add; basket target/stop monitored during the minute; broker Bid/Ask fills
- 2026-08-29 bot24-specific decision: the intended contract is now confirmed-M1 entry/add and confirmed-M1 basket exit evaluation, filled from the first available executable Bid/Ask after the new confirmed bar. The process may poll every 5 seconds for synchronization, but it does not re-evaluate exits within the same confirmed M1 bar.
- 2026-08-31 entry-routing refactor: the frozen UTC 13:00-18:00 admission window and the existing signal are evaluated through the shared pure time-regime wrapper. This is an implementation-parity refactor only; it does not authorize a new time block, alter the signal, or change position lifecycle and execution semantics.
- Parity status: the 2026-08-20 failure remains valid evidence against the abandoned intraminute contract. It no longer defines bot24's intended execution clock.
- Tick-exit reassessment: `man_028_v005` / `bot24_visual_no_adverse_c_tick_exit_v1`, full dev 2026-04-15 through 2026-07-17. Base PnL -143.534, PF 0.91466, closed MDD 390.472; stress PnL -276.826, PF 0.83983, closed MDD 487.119. Decision: `dev_rejected`; holdout not read.
- Controlled exit ablation reproduced the historical result exactly. Every-tick trigger with the historical next-minute fill remained positive at PnL 162.354 / PF 1.10253. Changing only to trigger-tick fill reduced PnL by 287.146 to -124.792 / PF 0.92558. The historical edge therefore depended materially on post-trigger next-minute holding and is not evidence for immediate live exits.

Frozen live mapping: UTC 13-18; breakout plus 10-bar impulse; impulse ATR
0.60; 30-bar breakout; ATR30/ATR90 volume ratio minimum 1.05; favorable adds
at 0.45 ATR30; maximum 8 positions; basket target USD 16; basket stop USD 48;
maximum hold 120 M1 bars; cooldown 3 bars.

Historical M1-exit dev: PnL 326.378, PF 1.20840, DD 294.609, 307 closed tickets. Historical M1-exit reusable
evaluation: PnL 115.704, PF 1.34541, DD 114.823, 83 closed tickets. These figures support the newly explicit confirmed-M1 contract, but deployment still requires source/state reconciliation and local validation; they do not authorize deployment by themselves.
Backtest costs include observed spread and adverse slippage; live execution uses
broker fills and stores broker-confirmed entry/close prices.

## Runner shadow benchmark (2026-08-29)

The passive runner uses the pre-existing `S_cap2_atr85_t32_s48` structure only
as a shadow benchmark; it is not promoted to live. Under the observed
confirmed-M1/next-tick execution contract, full dev results were PnL 624.452,
PF 1.46583 and every-tick MTM DD 159.244 in base costs, and PnL 564.993,
PF 1.43310 and MTM DD 169.861 in stress costs. Stress was positive in all four
dev months. The unchanged core comparison was PnL 314.474 / MTM DD 312.663 in
base and PnL 66.040 / MTM DD 323.740 in stress.

The untouched 2026-08-25 through 2026-08-28 forward file was read only after
the candidate was fixed. Runner results were PnL 124.257 / MTM DD 80.028 in
base and PnL 37.1865 / MTM DD 107.247 in stress. The unchanged core was PnL
128.791 / MTM DD 83.798 in base and PnL 76.821 / MTM DD 84.522 in stress.
Therefore this is an evidence-collection candidate, not an authorization to
replace the live core or to deploy the current local runner.
